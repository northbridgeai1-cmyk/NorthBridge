{
  "_comment": "MCP servers for Cursor. Keys are NOT stored here: export TWENTY_FIRST_API_KEY and GITHUB_PERSONAL_ACCESS_TOKEN in your shell and Cursor passes them through. Safe to commit.",
  "mcpServers": {
    "shadcn-ui": {
      "command": "npx",
      "args": ["-y", "@jpisnice/shadcn-ui-mcp-server"],
      "env": {
        "GITHUB_PERSONAL_ACCESS_TOKEN": "${env:GITHUB_PERSONAL_ACCESS_TOKEN}"
      }
    },
    "21st-magic": {
      "command": "npx",
      "args": ["-y", "@21st-dev/magic@latest"],
      "env": {
        "API_KEY": "${env:TWENTY_FIRST_API_KEY}"
      }
    }
  }
}
